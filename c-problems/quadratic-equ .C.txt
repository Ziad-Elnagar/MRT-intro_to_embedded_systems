#include <stdio.h>
#include <math.h>
int main()
{
 double a,b,c,discriminant,R1,R2 ;
 printf("a?\n");
 printf("b?\n");
 printf("c?\n");
 scanf("%lf %lf %lf",&a,&b,&c);
 discriminant = b * b - 4 * a * c ;
 if (discriminant > 0) {
    R1= (-b + sqrt(discriminant)) / (2 * a);
 R2 = (-b - sqrt(discriminant)) / (2 * a);
    printf("The roots are: %lf and %lf.\n", R1, R2); }
    
    else if (discriminant == 0) {
  R1 = R2 = -b / (2 * a);
    printf("The quadratic equation has one real root: %lf.\n", R1);
  } else {
    printf("The quadratic equation has no real roots.\n");
  }
    
    
    
    
    
    
}
    
    
    
    